const Footer = () => (
  <footer className="absolute bottom-0 left-0 bg-black opacity-80 flex items-center justify-center w-full mt-auto h-24 border-t">
    <div>Footer</div>
  </footer>
);

export default Footer;
